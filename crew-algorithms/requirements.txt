# Python dependencies for the WILDFIRE algorithm image (installed by Dockerfile.algorithm).
# mlagents_envs is installed separately from git in the Dockerfile.

# Unity <-> Python bridge (TorchRL environment wrapper around ML-Agents)
torch==2.2.0
torchrl==0.3.0
tensordict==0.3.0
numpy==1.23.5
protobuf==3.19.6
grpcio==1.48.2

# Configuration (hydra structured configs)
hydra-core==1.3.2
omegaconf==2.3.0
attrs==22.2.0
cattrs==22.2.0

# LLM client + token counting (token_guard, used by the baseline algorithms)
openai==1.82.0
pydantic==2.11.5
tiktoken==0.6.0
certifi

# HTTP service
fastapi
uvicorn
requests

# Image handling (observation images for the interface, render.mp4)
pillow
opencv-python-headless==4.11.0.86
